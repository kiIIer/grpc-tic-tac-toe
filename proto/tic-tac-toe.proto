syntax = "proto3";
package ttt;

service Game {
    rpc Connect (stream Request) return (stream Responce);
}

message Request {
    string type = 1;
    repeated int8 payload = 2;
}

message Responce {
    string type = 1;
    repeated int8 payload = 2;
}
